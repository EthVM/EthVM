type FavToken {
    address: String!
    name: String!
}
extend type Query {
    checkToken(hash: String!): FavToken
    favTokens: [FavToken!]!
}

extend type Mutation {
    addFavToken(hash: String!, name: String): FavToken!
    deleteFavToken(hash: String!, name: String): FavToken
}

mutation addFaveToken($address: String!, $symbol: String) {
    addFaveToken(address: $address, symbol: $symbol) @client {
        symbol
    }
}

mutation deleteFaveToken($address: String!) {
    deleteFaveToken(address: $address) @client {
        symbol
    }
}

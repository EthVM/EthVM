mutation addFavToken($address: String!, $symbol: String) {
    addFavToken(hash: $address, symbol: $symbol) @client {
        symbol
    }
}

mutation deleteFavToken($address: String!) {
    deleteFavToken(hash: $address) @client {
        symbol
    }
}

query getBlockTransfers($_number: Int) {
    getBlockTransfers(number: $_number) {
        transfers {
            transfer {
                transactionHash
                to
                block
                timestamp
                from
                txFee
            }
            value
        }
    }
}

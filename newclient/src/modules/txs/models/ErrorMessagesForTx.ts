export enum ErrorMessageTx {
    details = 'message.error.tx.detail',
    notFound = 'message.tx.not-exist'
}

export enum TxStatus {
    success = 'common.success',
    failed = 'common.fail',
    pending = 'common.pending',
    replaced = 'common.replaced'
}

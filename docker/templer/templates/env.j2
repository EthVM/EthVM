# DB
DATABASE_NAME={{ DATABASE_NAME }}
DATABASE_USER={{ DATABASE_USER }}
DATABASE_PASSWORD={{ DATABASE_PASSWORD }}
JDBC_URL='{{ JDBC_URL }}'

# Parity
PARITY_ENABLED={{ PARITY_ENABLED|lower }}
{% if PARITY_ENABLED %}
PARITY_CHAIN={{ PARITY_CHAIN }}
PARITY_ENABLE_BIND_MOUNTPOINT={{ PARITY_ENABLE_BIND_MOUNTPOINT|lower }}
PARITY_BIND_MOUNTPOINT={{ PARITY_BIND_MOUNTPOINT }}
PARITY_INSTA_MINING={{ PARITY_INSTA_MINING|lower }}
PARITY_MIN_PEERS={{ PARITY_MIN_PEERS }}
PARITY_MAX_PEERS={{ PARITY_MAX_PEERS }}
{% endif %}

# Kafka
KAFKA_ENABLED={{ KAFKA_ENABLED|lower }}
{% if KAFKA_ENABLED %}
KAFKA_ZOOKEEPER_CONNECT={{ KAFKA_ZOOKEEPER_CONNECT }}
KAFKA_BOOTSTRAP_SERVERS={{ KAFKA_BOOTSTRAP_SERVERS }}
KAFKA_SCHEMA_REGISTRY_URL={{ KAFKA_SCHEMA_REGISTRY_URL }}
KAFKA_CONNECT_URL={{ KAFKA_CONNECT_URL }}
{% if KAFKA_ENABLE_BIND_MOUNTPOINT %}
KAFKA_BIND_MOUNTPOINT={{ KAFKA_BIND_MOUNTPOINT }}
ZOOKEEPER_BIND_MOUNTPOINT={{ ZOOKEEPER_BIND_MOUNTPOINT }}
{% endif %}
{% endif %}

{% if TIMESCALE_ENABLE_BIND_MOUNTPOINT %}
# Timescale
TIMESCALE_BIND_MOUNTPOINT={{ TIMESCALE_BIND_MOUNTPOINT }}
{% endif %}

scalar Date
scalar JSON
scalar Decimal
scalar Buffer
scalar StatisticValue
scalar Long
scalar BigNumber

enum FilterEnum {
  in, out, all
}


enum Duration {
  ALL,
  YEAR,
  MONTH,
  WEEK,
  DAY
}

# We have to do this as we have no mutations yet and it was preventing client side definitions

type Mutation {
  foo: Boolean
}


type Query {
  search(query: String!): Search!
}

type Search {
  type: SearchType!
  address: Account
  block: Block
  uncle: Uncle
  tx: Transaction
  tokens: [TokenSearch]
}

enum SearchType {
  Address,
  Block,
  Uncle,
  Tx,
  Token,
  None
}

type TokenSearch {
  address: String!
  symbol: String
  name: String
  currentPrice: BigNumber
  logo: String
  website: String
}

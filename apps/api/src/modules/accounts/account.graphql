type Query {
  accountByAddress(address: String!): Account
}

type Account {
  address: String
  balance: Float
  totalTxCount: String
  inTxCount: String
  outTxCount: String
  isMiner: Boolean
  isContractCreator: Boolean
}

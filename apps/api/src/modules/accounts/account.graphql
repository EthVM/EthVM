type Query {
  accountByAddress(address: String!): Account
}

type Account {
  address: String
  balance: String
  totalTxCount: String
  inTxCount: String
  outTxCount: String
  isMiner: Boolean
  isContractCreator: Boolean
}

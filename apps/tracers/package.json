{
  "name": "tracers",
  "version": "1.0.0",
  "description": "Tracers used by Go-Ethereum client to trace transactions",
  "main": "tracer.js",
  "author": "enKryptIO Team",
  "license": "MIT",
  "scripts": {
    "build": "yarn build:clean && uglifyjs --compress --mangle -b beautify=false -b quote_style=1 --output out/tracer.js -- tracer.js",
    "build:clean": "rm -rf out && mkdir out",
    "test:unit": "jest --config jest.unit.json",
    "test:cov": "jest --coverage --config jest.unit.json",
    "prettier": "prettier --write 'tracer.js'"
  },
  "devDependencies": {
    "jest": "^23.5.0",
    "mocha": "^5.2.0",
    "prettier": "1.14.2",
    "uglify-js": "^3.4.9"
  }
}
